const PrimaryBackground = `rgb(10,25,47)`;

const PrimaryColor = `rgb(204, 214, 246)`;
const PrimaryColorWithOpacity = (opacity) => {
  return `rgb(204, 214, 246, ${opacity})`;
}

const SecondaryColor = `rgb(100, 255, 218)`;
const SecondaryColorWithOpacity = (opacity) => {
  return `rgb(100, 255, 218, ${opacity})`;
}

export {
  PrimaryBackground,
  PrimaryColor,
  PrimaryColorWithOpacity,
  SecondaryColor,
  SecondaryColorWithOpacity
}